


#spring.application.name=ms-config-server
server:
  port: 7070




spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/keinraf/DAD-2025-E # ruta del repositorio git
          searchPaths: config-data
          default-label: principal
  security:
    user:
      name: root
      password: 123456